# Generated by AI
version: "3.8"

services:
  astro:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4101:4321"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
