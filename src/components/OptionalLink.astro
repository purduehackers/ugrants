---
import Link from "@components/Link.astro";

interface Props {
    href?: string;
}

const { href } = Astro.props;
---

{
    href ? (
        <Link href={href}>
            <slot />
        </Link>
    ) : (
        <>
            <slot />
        </>
    )
}
